# X thread — scope before build (v1.17)

**Status:** Draft, not yet posted. Replaces `x-thread.md`, which still says 23 agents.

**Media:** tweet 1 gets `launch/demo-apex-plan.png`. X does not render SVG.

## Tweet 1

```
Ask a coding agent to "add auth" and it decides the size of the job
silently, while it works.

So I made mine quote first: tiers, which specialists each one uses,
tokens, rough cost. I pick, then it builds.

Real output for Next.js + Postgres auth:
```

## Tweet 2

```
The part I like most: it refuses options.

No XS tier for auth, because skipping the security review isn't
acceptable there. A menu that offers everything isn't advice.
```

## Tweet 3

```
Behind it are 100 specialist agents (backend, security, data,
frontend, QA, infra, product, legal...), all plain markdown.

A session hook hides the skills your repo doesn't need, so you don't
pay for 429 skill descriptions in every session.
```

## Tweet 4

```
MIT, free, Claude Code plugin:

claude plugin marketplace add tonone-ai/tonone
claude plugin install tonone@tonone-ai

Then /apex-plan <your task>

github.com/tonone-ai/tonone
```
